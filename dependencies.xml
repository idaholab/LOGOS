<dependencies>
  <main>
    <!-- fix the version of pyomo, this is because, in the version after 5.7.3, PySP package is moved to a independent
    lib. See https://pyomo.readthedocs.io/en/latest/modeling_extensions/stochastic_programming.html
    We need to update LOGOS to use either PySP or mpi-sppy, using 5.7 which will consistent with HERON-->
    <pyomo source="pip">6.4</pyomo>
    <coincbc os="mac,linux" skip_check="True" source="forge">2.10.5</coincbc>
    <!-- Windows doesn't have coincbc, so get it through cbcpy? cbcpy doesn't exist in python newer than 3.7, so this is not a good solution. The suggestion to use cbcpy was in https://stackoverflow.com/questions/58868054/how-to-install-coincbc-using-conda-->
    <glpk skip_check="True" source="forge" />
    <pyutilib source='pip'/>
    <numpy-financial source='pip'/>
    <networkx source='pip'/>
    <plotly source='pip'/>
    <pandas source='pip'/>
    <!-- Test / optional-algorithm deps. These are installed into the LOGOS env
         (so the pytest suite runs in CI and the GA/ALNS backends are available),
         but marked skip_check so RAVEN's startup library check does NOT fail when
         a plugin consumer's environment (e.g. a plain `raven_framework` run in
         the raven_libraries env) lacks them. They are never imported to run a
         model, only by the test suite and the optional metaheuristics. -->
    <deap source='pip' skip_check="True"/>
    <pytest source="pip" skip_check="True"/>
    <hypothesis source='pip' skip_check="True"/>
    <alns source='pip' skip_check="True"/>
  </main>

  <alternate name="pip">
    <coincbc skip_check="True">remove</coincbc>
    <glpk skip_check="True">remove</glpk>
    <ipopt skip_check="True">remove</ipopt>
    <plotly source='pip'/>
    <networkx source='pip'/>
    <!-- <cbcpy source='pip'/> -->
  </alternate>
</dependencies>

