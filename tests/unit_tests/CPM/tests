[Tests]

 [./cpm_pytest_suite]
  # Runs the full CPM pytest suite (~840 tests) via a shim; see
  # run_cpm_pytests.py.  Optional-dependency tests (deap / alns) self-skip.
  type  = 'RavenPython'
  input = 'run_cpm_pytests.py'
 [../]

[]
