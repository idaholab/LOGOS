[pytest]
# Add the src/ directory to sys.path so that "from CPM.pert import ..."
# resolves to the source package (src/CPM) without installing it.
pythonpath = ../../../src
testpaths  = .
addopts    = -v --tb=short
markers    =
    slow: marks tests as slow (performance / microbenchmark)
