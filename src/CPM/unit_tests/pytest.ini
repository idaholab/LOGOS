[pytest]
# Add the CPM parent directory to sys.path so that "from CPM.pert import ..."
# works without installing the package.
pythonpath = ../..
testpaths  = .
addopts    = -v --tb=short
markers    =
    slow: marks tests as slow (performance / microbenchmark)
